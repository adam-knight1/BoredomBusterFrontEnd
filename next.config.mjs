// next.config.mjs

 //@type {import('next').NextConfig}
const nextConfig = {
  images: {
    domains: ['api.api-ninjas.com', 'anotherdomain.com'],
  },
  // other config
};

export default nextConfig;
